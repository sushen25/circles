# Circles

Codename only — nothing in code, domains or store metadata should assume the
final name (architecture §5.4).

A small group keeps saying "we should catch up" and never does. Circles turns
one link, pasted into the group chat, into a real time in the diary: friends
mark the times they would genuinely be up for — **no account, no app, no
calendar access** — and the organiser confirms.

**Working here? Read [`AGENTS.md`](AGENTS.md) first.** It is short, and it is
the authoritative list of rules, commands and boundaries.

## Setup

Node 22 (see `.nvmrc`), pnpm 9 via Corepack, Docker, and `psql`.

```bash
corepack enable
pnpm i
pnpm db:start     # local Supabase in Docker: migrations, seed, Mailpit
pnpm check        # everything CI runs; expect it to pass on a clean clone
pnpm dev:web      # the web app at http://localhost:8081
```

**The app only talks to the local database once `apps/app/.env.local` exists.**
Without it every screen renders fixtures, which is how the design gallery works
and is easy to mistake for a broken backend. The one command that writes it, the
seed data, signing in locally, the test suites and what to do when the stack
misbehaves are in **[`docs/runbooks/local.md`](docs/runbooks/local.md)**.

Native has caveats worth reading before you start:
[`apps/app/README.md`](apps/app/README.md).

## What is where

| Path                 |                                                                                   |
| -------------------- | --------------------------------------------------------------------------------- |
| `apps/app`           | the Expo universal app — web, iOS, Android from one codebase                      |
| `packages/domain`    | the product's rules as pure TypeScript, shared with the server                    |
| `packages/contracts` | Zod schemas for every boundary, the analytics catalogue, generated database types |
| `packages/tokens`    | design tokens generated from the canvas, and the two typefaces                    |
| `packages/config`    | brand constants, feature flags, environment schema                                |
| `supabase/`          | migrations, database tests, Edge Functions                                        |
| `docs/`              | the spec, the architecture, the ADRs, the design canvas                           |
| `docs/runbooks/`     | what to do when something breaks                                                  |

## The documents

- [Product spec](docs/mvp-product-spec.md) — what it does and why.
- [Technical architecture](docs/technical-architecture.md) — how it is built.
- [Design manifesto](docs/design-manifesto.md) — how it looks and speaks. The
  canvas is generated by [`docs/design/gen.py`](docs/design/gen.py); the
  artboards are never edited by hand.
- [Decisions](docs/decisions/README.md) — every rule that changed, and why.
- [Working process](docs/working-process.md) — how a ticket gets worked, and the
  rules that came out of getting it wrong.

## Where it runs

|       | Supabase            | Web                                    | Native                |
| ----- | ------------------- | -------------------------------------- | --------------------- |
| local | CLI stack in Docker | `pnpm dev:web`                         | development build     |
| dev   | `circles-dev`       | `sushen25s-team-circles--dev.expo.app` | `development` channel |
| prod  | `circles-prod`      | `meet.sushensatturu.com`               | `production` channel  |

A merge to `main` pushes migrations, deploys the Edge Functions and the web
build to `dev`, and publishes an update to the `development` channel. `prod` is
deployed by hand (`deploy-prod`, which waits for approval) and is not yet
released to anybody but the founder.

[`docs/runbooks/local.md`](docs/runbooks/local.md) is how to run it on your
machine; [`docs/runbooks/environments.md`](docs/runbooks/environments.md) is the
reference for what is where in `dev` and `prod`.
