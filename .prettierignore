node_modules/
dist/
*.tsbuildinfo
pnpm-lock.yaml

# hand-written prose and the generated design canvas
docs/
.claude/

# generated native projects and Expo caches (expo prebuild — see apps/app/README.md)
apps/*/android/
apps/*/ios/
apps/*/.expo/

# generated by `pnpm gen:types` from the live schema — formatting it here would
# fight `check:types`, which diffs the generator's own output
packages/contracts/src/db.generated.ts

# written into the workspace by the gitleaks step in CI
*.sarif

# written by the Supabase CLI whenever it links a project — `supabase link`,
# and `secrets list`/`db push` against a `--project-ref`. `supabase/.gitignore`
# already keeps it out of git, so without this line `pnpm check` fails on a
# file nobody is allowed to commit, and `prettier --write` reformats something
# the CLI rewrites on its next run.
supabase/.temp/

# rendered email, written by the snapshot tests exactly as it is sent — each
# `.html` is what the provider receives, and reformatting it would snapshot
# something nobody sends
supabase/functions/_shared/email/__snapshots__/
