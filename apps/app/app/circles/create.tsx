import { CreateCircleFlow } from '../../src/features/circles/CreateCircleFlow';

/** Route only — thin composition, no logic (architecture §7.1). */
export default function Route() {
  return <CreateCircleFlow />;
}
